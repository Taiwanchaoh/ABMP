function Nav = Navigation(Fleet)
% This function will guide the Air Vehicle flying to its destination. It
% will neglect every other air vehicle.
% Outputs: Nav[1 by 3] = [vx,vy,vz]
%%

% Fleet = [type,Position,Volicity,Statue,size,Dest_type,Dest,TMission]
% Dest_type = 0 (Delivery Destination), = 1 (Landing Path), = 2 (Take-off)

if Fleet.Type == 1
    % Navigation of UAV
    
else
    % Navigation of GA
    
end

end